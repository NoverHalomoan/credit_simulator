# Credit Simulator (Console App)

A simple vehicle credit installment simulator built with Java and Maven.

## 🚀 How to Run

### Locally (with Java 21)

```bash
mvn clean package
java -jar target/credit_simulator-jar-with-dependencies.jar
```
